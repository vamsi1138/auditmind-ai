export async function resolveFromAddress(contractAddress: string): Promise<string> {
  throw new Error(
    `Contract address input is not implemented yet. Received address: ${contractAddress}`
  );
}